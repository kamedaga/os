# ウィンドウシステム（Step 1）

## 実装範囲

現状のコンポジターは以下を実装しています。

- 背景を `#FDFDFD` で描画
- 仮想Framebuffer内容を解析して、非背景領域ごとにウィンドウを自動生成
- ヘッダーのドラッグでウィンドウ位置変更
- 円形 `×` ボタンでウィンドウを閉じる（非表示化のみ）
- 共有メタデータページからウィンドウ名を表示
- 微量の角丸表現

この段階では「閉じたウィンドウを再度開く動作」は未実装です。

## 見た目仕様

- ヘッダー背景: `#F6F5F4`
- ヘッダー文字: `#D7D7D6`
- 枠線: `#D8D7D6`
- コンテンツ背景: `#FFFFFF`

角丸は四隅を2pxだけカットする軽量な方式で実装しています。  
`×` ボタンは円形ヒットテストで判定します。

## 共有メタデータ（ウィンドウ名）

ウィンドウ名は以下の共有ページからコンポジターが読み取ります。

- 仮想アドレス: `0x3C00_7000`
- マジック: `0x574D455441`（`"WMETA"`）
- シーケンス番号: `+8` オフセット（u64）
- タイトル文字列: `+16` オフセット（最大64バイト、NUL終端）

アプリ側がタイトル更新後にシーケンス番号を加算し、コンポジター側が変更を検知してヘッダー文字列を再描画します。

## 現在のマッピング

- Process0（マウスドライバ）: 共有ページのオーナー
- Process1（コンポジター）: ウィンドウメタを読み取り専用でマップ
- Process2 / Process4（デモアプリ）: ウィンドウメタを読み書きでマップ

仮想Framebufferは共有されたまま、コンポジターで最終合成されます。

## 描画方針

- ハードウェアFramebufferは読み取らない
- コンポジター内部のバックバッファを正としてFramebufferへ書き込み
- マウスカーソルは最終出力時に重畳
- 32x32の仮想Framebuffer内容を走査し、背景色(`0x00FD_FDFD`)と0値以外を「有効領域」として横方向クラスタ分割
- 分割された各クラスタをそれぞれ独立ウィンドウに拡大合成
