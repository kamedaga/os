# default boot is intentionally small
# shell and init are loaded directly from disk
block_driver /bin/virtio_blk.elf
persistent_fs /bin/persistent_fs.elf
# block_demo is a shell builtin that connects to the registered block service
