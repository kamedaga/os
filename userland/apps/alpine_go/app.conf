schema_version = 1

[app]
id = "alpine_go"
kind = "file"
role = "asset"

[source.file]
path = ".artifacts/userland-fixtures/alpine-go-root"
rebuild_tool = "wsl"
rebuild_dir = "."
rebuild_args = ["-e", "bash", "tools/build_wsl_alpine_go.sh"]

[build]
format = "tree"
target = ""
optimize = "release"
output_name = "ROOT"
strip = false

[[publish]]
id = "rootfs"
fs = "rootfs"
path = "/"
