schema_version = 1

[app]
id = "os_release"
kind = "file"
role = "asset"

[source.file]
path = "userland/fixtures/base/os-release"

[build]
format = "blob"
target = ""
optimize = "release"
output_name = "OS-RELEASE"
strip = false

[[publish]]
id = "rootfs"
fs = "rootfs"
path = "/etc/os-release"
