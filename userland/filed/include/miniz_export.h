#pragma once

/* miniz normally generates this export header from CMake. filed links its
 * unmodified inflater sources into one executable, so no symbol export is
 * required. */
#define MINIZ_EXPORT
