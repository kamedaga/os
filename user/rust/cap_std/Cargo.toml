[package]
name = "cap_std"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
rt_core = { path = "../rt_core" }
rt_handle = { path = "../rt_handle" }
rt_io = { path = "../rt_io" }
